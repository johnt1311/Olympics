DROP TABLE stats;
CREATE TABLE stats (
	COUNTRY VARCHAR,
	COUNTRY_CODE VARCHAR,
	GOLD INT,
	SILVER INT,
	BRONZE INT,
	TOTAL INT,
	YEAR INT,
	HOST_CITY VARCHAR,
	HOST_COUNTRY VARCHAR,
	COU VARCHAR,
	LIFE_EXPECTANCY DECIMAL,
	POPULATION DECIMAL);

SELECT * FROM stats;

DROP TABLE Gloc;
CREATE TABLE Gloc (
		Country VARCHAR, 
		Capital VARCHAR, 
	 	GDP FLOAT,
		Lat_Hemisphere VARCHAR,
       	LonG_hemisphere VARCHAR,
		DistT1964 DECIMAL,
        DistM1968 DECIMAL, 
		DistM1972 DECIMAL,
        DistM1976 DECIMAL, 
		DistM1980 DECIMAL,
       	DistL1984 DECIMAL, 
		DistS1988 DECIMAL,
        DistB1992 DECIMAL, 
		DistA1996 DECIMAL,
        DistS2000 DECIMAL, 
		DistA2004 DECIMAL,
        DistB2008 DECIMAL, 
		DistL2012 DECIMAL,
        DistR2016 DECIMAL, 
		DistT2020 DECIMAL,
        Year FLOAT,
       	Value DECIMAL);
		
SELECT * FROM gloc;